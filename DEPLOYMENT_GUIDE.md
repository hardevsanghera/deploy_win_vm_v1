dep# VM Deployment Instructions

## Overview
The script now supports two modes:
1. **Resource Selection Mode** (default) - Select cluster, subnet, image, and sysprep file
2. **Deployment Mode** (`--deploy` flag) - Deploy VM using saved configuration

## Phase 1: Resource Selection
```powershell
python deploy_win_vm.py <PC_IP> <USERNAME>
```
Example:
```powershell
python deploy_win_vm.py 10.38.1.202 admin
```

This will:
- Connect to Prism Central
- Display available clusters, subnets, images, and sysprep files
- Save your selections to `deployment_config.json`

## Phase 2: VM Deployment
```powershell
python deploy_win_vm.py --deploy
```

This will:
1. Load the saved `deployment_config.json` configuration
2. Read the `create_vm_SKEL.json` template
3. Replace placeholders with actual UUIDs:
   - `XXSUBNETUUIDXX` → Selected subnet UUID
   - `XXIMAGEUUIDXX` → Selected image UUID
   - `XXCLUSTERUUIDXX` → Selected cluster UUID
   - `XXVMNAMEXX` → "WINVM-A"
   - `XXUSERDATAXX` → Base64-encoded sysprep XML content
4. Make POST call to `https://<PC_IP>:9440/api/nutanix/v3/vms`
5. Display VM creation results and task UUID

## Files Required
- `deployment_config.json` (generated by resource selection)
- `create_vm_SKEL.json` (VM template)
- Sysprep XML file (selected during resource selection)

## Security Notes
- **SSL Certificate Verification**: Disabled (`verify=False`) for all API calls
- **Certificate Warnings**: Automatically suppressed for self-signed certificates
- **Authentication**: Uses HTTP Basic Auth with secure password input
- **Credentials**: Not stored in any files, entered interactively

## Output
Upon successful deployment:
- VM UUID
- Task UUID for monitoring progress
- VM status information

The VM will be created with:
- Name: WINVM-A
- Memory: 8096 MB
- CPUs: 1 socket × 4 cores
- Power State: ON
- Custom sysprep configuration applied